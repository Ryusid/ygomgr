apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ygomgr
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: ygomgr
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: backend
