apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ygomgr
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: ygomgr
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: frontend
